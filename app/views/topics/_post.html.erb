

<%= @markdown.render(post.body).html_safe %>

<% if user_signed_in? %>

  <% if current_user == post.user %>
    <%= link_to "Edit", edit_topic_post_path(@topic, post) %>
  <% end %>
  
  <%= link_to "Bookmark", new_post_bookmark_path(post) %>
  <%= render partial: "arrows", locals: { post: post } %>

<% end %>
