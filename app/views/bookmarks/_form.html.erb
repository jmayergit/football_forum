<%= form_for [@post, @bookmark] do |f| %>

  <%= f.hidden_field :user_id, value: current_user.id %>

  <%= f.hidden_field :post_id, value: params[:post_id] %>

  <%= f.submit "Create Bookmark?" %>
<% end %>
